//! Not done
struct Solution;
impl Solution {
    pub fn maximum_score(a: i32, b: i32, c: i32) -> i32 {
        let game = true;
        let game_vec = [a, b, c].to_vec();
        while game {}
        8
    }
}
